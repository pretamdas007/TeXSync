# Server Environment Variables (for Render.com)
NODE_ENV=production
PORT=10000

# Render External URL (Replace with your actual Render service URL)
RENDER_EXTERNAL_URL=https://texsync.onrender.com

# Security
CORS_ORIGIN=https://texsync.vercel.app

# LaTeX Compilation Settings
MAX_COMPILATION_TIME=30000
TEMP_DIR=./temp

# Optional: Database (if you add persistent storage later)
# DATABASE_URL=your_database_url
